diff --git a/node_modules/vaul/dist/index.js b/node_modules/vaul/dist/index.js
index 7f343be..b62648d 100644
--- a/node_modules/vaul/dist/index.js
+++ b/node_modules/vaul/dist/index.js
@@ -1135,6 +1135,7 @@ function Root({ open: openProp, onOpenChange, children, onDrag: onDragProp, onRe
         var _window_visualViewport;
         function onVisualViewportChange() {
             if (!drawerRef.current || !repositionInputs) return;
+            if (drawerRef.current.hasAttribute("data-quad-vv-managed")) return;
             const focusedElement = document.activeElement;
             if (isInput(focusedElement) || keyboardIsOpen.current) {
                 var _window_visualViewport;
@@ -1167,7 +1168,10 @@ function Root({ open: openProp, onOpenChange, children, onDrag: onDragProp, onRe
                     }
                     // When fixed, don't move the drawer upwards if there's space, but rather only change it's height so it's fully scrollable when the keyboard is open
                     if (fixed) {
-                        drawerRef.current.style.height = `${height - Math.max(diffFromInitial, 0)}px`;
+                        const base = initialDrawerHeight.current || height;
+                        const fixedInset = Math.max(diffFromInitial, 0);
+                        const fixedNext = Math.round(base - fixedInset);
+                        drawerRef.current.style.height = `${Math.max(120, fixedNext)}px`;
                     } else {
                         drawerRef.current.style.height = `${Math.max(newDrawerHeight, visualViewportHeight - offsetFromTop)}px`;
                     }
@@ -1298,6 +1302,7 @@ function Root({ open: openProp, onOpenChange, children, onDrag: onDragProp, onRe
     React__namespace.default.useEffect(()=>{
         // Trigger enter animation without using CSS animation
         if (isOpen) {
+            initialDrawerHeight.current = 0;
             set(document.documentElement, {
                 scrollBehavior: 'auto'
             });
diff --git a/node_modules/vaul/dist/index.mjs b/node_modules/vaul/dist/index.mjs
index 0dbbce5..763c010 100644
--- a/node_modules/vaul/dist/index.mjs
+++ b/node_modules/vaul/dist/index.mjs
@@ -1113,6 +1113,7 @@ function Root({ open: openProp, onOpenChange, children, onDrag: onDragProp, onRe
         var _window_visualViewport;
         function onVisualViewportChange() {
             if (!drawerRef.current || !repositionInputs) return;
+            if (drawerRef.current.hasAttribute("data-quad-vv-managed")) return;
             const focusedElement = document.activeElement;
             if (isInput(focusedElement) || keyboardIsOpen.current) {
                 var _window_visualViewport;
@@ -1145,7 +1146,10 @@ function Root({ open: openProp, onOpenChange, children, onDrag: onDragProp, onRe
                     }
                     // When fixed, don't move the drawer upwards if there's space, but rather only change it's height so it's fully scrollable when the keyboard is open
                     if (fixed) {
-                        drawerRef.current.style.height = `${height - Math.max(diffFromInitial, 0)}px`;
+                        const base = initialDrawerHeight.current || height;
+                        const fixedInset = Math.max(diffFromInitial, 0);
+                        const fixedNext = Math.round(base - fixedInset);
+                        drawerRef.current.style.height = `${Math.max(120, fixedNext)}px`;
                     } else {
                         drawerRef.current.style.height = `${Math.max(newDrawerHeight, visualViewportHeight - offsetFromTop)}px`;
                     }
@@ -1276,6 +1280,7 @@ function Root({ open: openProp, onOpenChange, children, onDrag: onDragProp, onRe
     React__default.useEffect(()=>{
         // Trigger enter animation without using CSS animation
         if (isOpen) {
+            initialDrawerHeight.current = 0;
             set(document.documentElement, {
                 scrollBehavior: 'auto'
             });
